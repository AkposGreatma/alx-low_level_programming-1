#include <stdio.h>

/**
* main - Entry point
* Return: 0
*/
int main(void)
{
	char g;
	char o;

	for (g = 'a'; g <= 'z'; g++)
	{
	putchar(g);
	}
	for (o = 'A'; o <= 'Z'; o++)
	{
	putchar(o);
	}

	return (0);
}
